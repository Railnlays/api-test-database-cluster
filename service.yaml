apiVersion: v1
kind: Service
metadata:
  name: api-test-database-cluster
  namespace: api
spec:
  type: LoadBalancer
  selector:
    app: api-test-database-cluster
  ports:
    - name: http
      port: 5000
      targetPort: 5000
      protocol: TCP